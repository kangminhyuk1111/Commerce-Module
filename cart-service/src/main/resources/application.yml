spring:
  application:
    name: Cart Service

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms
      password:
      lettuce:
        pool:
          max-active: 8
          max-wait: -1ms
          max-idle: 8
          min-idle: 0

server:
  port: 8085